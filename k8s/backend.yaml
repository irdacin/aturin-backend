apiVersion: apps/v1
kind: Deployment
metadata:
  name: aturin-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aturin-backend
  template:
    metadata:
      labels:
        app: aturin-backend
    spec:
      containers:
      - name: aturin-backend
        image: ghcr.io/irdacin/aturin-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        resources:
          requests:
            cpu: "100m"
          limits:
            cpu: "500m"
        env:
        - name: MONGO_URI
          value: "mongodb://fauzanaditya18juli:PasswordMongodb2025@cluster0-shard-00-00.b079d.mongodb.net:27017,cluster0-shard-00-01.b079d.mongodb.net:27017,cluster0-shard-00-02.b079d.mongodb.net:27017/diskusi?ssl=true&replicaSet=atlas-r6nw9g-shard-0&authSource=admin&retryWrites=true&w=majority&appName=Cluster0" 
        - name: PORT
          value: "5000"
---
apiVersion: v1
kind: Service
metadata:
  name: aturin-backend-service
spec:
  type: ClusterIP
  selector:
    app: aturin-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000